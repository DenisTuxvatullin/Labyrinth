#include <iostream>
#include <fstream>
#include <string>
#include <stdio.h>
#include <set>
#include <boost/chrono.hpp>

char map[100][100];
int newMap[100][100];
using namespace std;
int maxWidth = 0, maxHigh = 0;